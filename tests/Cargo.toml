[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
linkme = { workspace = true }
tester = { workspace = true }

trusted_kernel = {workspace = true }
pm = { path = "../src/pm" }
mm = { path = "../src/mm" }
